// Code your solution here
const findMatching = (drivers) {
let string;
drivers.filter(string => {
  return n % 2 === 0;
});
}

const superbowlWin = (arr) => {
  let year;
  arr.find(obj => {
    if (obj["result"] === "W") {
      year = obj.year;
    }
  })
  return year;
